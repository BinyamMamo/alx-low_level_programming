#include <stdio.h>

/**
 * main - Entry point of the program
 * @argc: argument count
 * @argv: argument array
 *
 * Return: always 0
 */
int __attribute__ ((unused)) main(int argc, char *argv[])
{
	printf("%s", argv[0]);
	return (0);
}
